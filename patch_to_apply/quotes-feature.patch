diff --git a/src/context/AppContext.tsx b/src/context/AppContext.tsx
index d4f97fc..8aa8632 100644
--- a/src/context/AppContext.tsx
+++ b/src/context/AppContext.tsx
@@ -12,6 +12,8 @@ import {
   StockMovement,
   SalesReturn,
   PriceHistory,
+  Quote,
+  QuoteItem,
   AuditLog,
   User,
   RolePermission,
@@ -68,6 +70,22 @@ interface AppContextType extends AppState {
     reason: string;
   }) => SalesReturn | null;
 
+  // Quotation Actions (non-posting rate quotes)
+  createQuote: (params: {
+    customerId: string;
+    items: QuoteItem[];
+    validUntil: string;
+    notes?: string;
+    status?: 'Draft' | 'Sent';
+    isZeroGst?: boolean;
+  }) => Quote;
+  updateQuote: (id: string, quoteData: Partial<Quote>) => void;
+  deleteQuote: (id: string) => boolean;
+  convertQuoteToSale: (
+    quoteId: string,
+    params: { amountPaid: number; paymentMethod: PaymentMethod }
+  ) => { sale: Sale; invoice: Invoice } | null;
+
   // Customer Actions
   addCustomer: (customerData: Omit<Customer, 'id' | 'totalPurchases' | 'totalPaid' | 'outstandingBalance' | 'createdAt' | 'updatedAt'>) => Customer;
   updateCustomer: (id: string, customerData: Partial<Customer>) => void;
@@ -159,6 +177,7 @@ const migrateBusinessProfile = (biz: BusinessProfile): BusinessProfile => ({
   nextPaymentNumber: biz.nextPaymentNumber ?? 1,
   nextPurchaseNumber: biz.nextPurchaseNumber ?? 1,
   nextReturnNumber: biz.nextReturnNumber ?? 1,
+  nextQuoteNumber: biz.nextQuoteNumber ?? 1,
   businessStateCode: biz.businessStateCode ?? biz.gstin?.substring(0, 2) ?? '27',
 });
 
@@ -176,6 +195,12 @@ export const AppProvider: React.FC<{ children: React.ReactNode }> = ({ children
         canDeleteSales: r.canDeleteSales ?? false,
         canModifyStock: r.canModifyStock ?? false,
       }));
+      // Give any role that already has Sales access the new Quotations module
+      saved.roles = saved.roles.map((r) =>
+        r.accessibleModules.includes('quotes') || !r.accessibleModules.includes('sales')
+          ? r
+          : { ...r, accessibleModules: [...r.accessibleModules, 'quotes'] }
+      );
       // Give admin all new permissions
       const adminRole = saved.roles.find((r) => r.id === 'role-admin' || r.name.toLowerCase().includes('admin'));
       if (adminRole) {
@@ -1098,6 +1123,142 @@ export const AppProvider: React.FC<{ children: React.ReactNode }> = ({ children
     return newReturn;
   };
 
+  // ----------------------------------------------------
+  // 3b. QUOTATIONS (non-posting rate quotes — never touch stock or khata)
+  // ----------------------------------------------------
+  const createQuote = (params: {
+    customerId: string;
+    items: QuoteItem[];
+    validUntil: string;
+    notes?: string;
+    status?: 'Draft' | 'Sent';
+    isZeroGst?: boolean;
+  }): Quote => {
+    const customer = stateRef.current.customers.find((c) => c.id === params.customerId);
+    if (!customer) throw new Error('Customer not found');
+
+    const isInterState = detectInterState(customer);
+    const totals = calculateSaleTotals(params.items, isInterState);
+    const quoteId = generateId('quote');
+    const quoteNum = `QTN-${new Date().getFullYear()}-${String(stateRef.current.business.nextQuoteNumber ?? 1).padStart(3, '0')}`;
+    const nowIso = new Date().toISOString();
+
+    const newQuote: Quote = {
+      id: quoteId,
+      quoteNumber: quoteNum,
+      customerId: customer.id,
+      customerName: customer.name,
+      customerPhone: customer.phone,
+      customerAddress: customer.address ? `${customer.address}, ${customer.city}` : '',
+      customerGstin: customer.gstin,
+      date: nowIso,
+      validUntil: params.validUntil,
+      items: params.items,
+      subtotal: totals.subtotal,
+      totalDiscount: totals.totalDiscount,
+      totalTax: totals.totalTax,
+      roundOff: totals.roundOff,
+      grandTotal: totals.grandTotal,
+      status: params.status || 'Draft',
+      isZeroGst: Boolean(params.isZeroGst),
+      isInterState,
+      notes: params.notes,
+      createdBy: stateRef.current.currentUser.name,
+      createdAt: nowIso,
+      updatedAt: nowIso,
+    };
+
+    const auditLog = addAuditLog(
+      'QUOTE_CREATED',
+      'Quote',
+      quoteNum,
+      `Created quotation ${quoteNum} for ${totals.grandTotal} for ${customer.name}`
+    );
+
+    setState((prev) => ({
+      ...prev,
+      quotes: [newQuote, ...(prev.quotes ?? [])],
+      auditLogs: [auditLog, ...prev.auditLogs],
+      business: { ...prev.business, nextQuoteNumber: (prev.business.nextQuoteNumber ?? 1) + 1 },
+    }));
+
+    return newQuote;
+  };
+
+  const updateQuote = (id: string, quoteData: Partial<Quote>) => {
+    const nowIso = new Date().toISOString();
+    const auditLog = addAuditLog(
+      'QUOTE_UPDATED',
+      'Quote',
+      id,
+      quoteData.status
+        ? `Quotation marked as ${quoteData.status}`
+        : `Updated quotation details`
+    );
+    setState((prev) => ({
+      ...prev,
+      quotes: (prev.quotes ?? []).map((q) => (q.id === id ? { ...q, ...quoteData, updatedAt: nowIso } : q)),
+      auditLogs: [auditLog, ...prev.auditLogs],
+    }));
+  };
+
+  const deleteQuote = (id: string): boolean => {
+    const quote = (stateRef.current.quotes ?? []).find((q) => q.id === id);
+    if (!quote) return false;
+
+    const auditLog = addAuditLog(
+      'QUOTE_DELETED',
+      'Quote',
+      quote.quoteNumber,
+      `Deleted quotation ${quote.quoteNumber} for ${quote.customerName}`
+    );
+    setState((prev) => ({
+      ...prev,
+      quotes: (prev.quotes ?? []).filter((q) => q.id !== id),
+      auditLogs: [auditLog, ...prev.auditLogs],
+    }));
+    return true;
+  };
+
+  /** Turn an accepted quote into a real sale (full pipeline: stock check, invoice, khata). */
+  const convertQuoteToSale = (
+    quoteId: string,
+    params: { amountPaid: number; paymentMethod: PaymentMethod }
+  ): { sale: Sale; invoice: Invoice } | null => {
+    const quote = (stateRef.current.quotes ?? []).find((q) => q.id === quoteId);
+    if (!quote || quote.status === 'Converted') return null;
+
+    // Reuse the complete sale pipeline (stock validation, invoice, payment, khata)
+    const { sale, invoice } = createSale({
+      customerId: quote.customerId,
+      items: quote.items,
+      amountPaid: params.amountPaid,
+      paymentMethod: params.paymentMethod,
+      isZeroGst: quote.isZeroGst,
+      notes: quote.notes ? `From quote ${quote.quoteNumber}. ${quote.notes}` : `From quote ${quote.quoteNumber}`,
+    });
+
+    const nowIso = new Date().toISOString();
+    const auditLog = addAuditLog(
+      'QUOTE_CONVERTED',
+      'Quote',
+      quote.quoteNumber,
+      `Converted quote ${quote.quoteNumber} into invoice ${sale.invoiceNumber}`
+    );
+
+    setState((prev) => ({
+      ...prev,
+      quotes: (prev.quotes ?? []).map((q) =>
+        q.id === quoteId
+          ? { ...q, status: 'Converted' as const, convertedSaleId: sale.id, updatedAt: nowIso }
+          : q
+      ),
+      auditLogs: [auditLog, ...prev.auditLogs],
+    }));
+
+    return { sale, invoice };
+  };
+
   // ----------------------------------------------------
   // 4. RECORD PAYMENT (Fixed: prev-based, overpayment validation, generateId, monotonic numbering)
   // ----------------------------------------------------
@@ -1759,6 +1920,10 @@ export const AppProvider: React.FC<{ children: React.ReactNode }> = ({ children
         deleteSale,
         deletePayment,
         processSalesReturn,
+        createQuote,
+        updateQuote,
+        deleteQuote,
+        convertQuoteToSale,
         addCustomer,
         updateCustomer,
         deleteCustomer,
@@ -1793,6 +1958,7 @@ export const AppProvider: React.FC<{ children: React.ReactNode }> = ({ children
         createDeliveryChallan,
         updateChallanStatus,
         deliveryChallans: state.deliveryChallans || [],
+        quotes: state.quotes ?? [],
       }}
     >
       {children}
diff --git a/src/data/mockData.ts b/src/data/mockData.ts
index 062d9ff..00c17fb 100644
--- a/src/data/mockData.ts
+++ b/src/data/mockData.ts
@@ -10,6 +10,7 @@ import {
   StockMovement,
   SalesReturn,
   PriceHistory,
+  Quote,
   AuditLog,
   User,
   RolePermission,
@@ -41,6 +42,7 @@ export const initialBusiness: BusinessProfile = {
   nextPaymentNumber: 1,
   nextPurchaseNumber: 1,
   nextReturnNumber: 1,
+  nextQuoteNumber: 4,
   invoiceTerms: '1. Goods once sold will be accepted for return within 7 days only with original invoice.\n2. Interest @ 18% p.a. will be charged if payment is delayed beyond agreed credit terms.\n3. Subject to Bengaluru jurisdiction.',
   currencySymbol: '₹',
   enableGst: true,
@@ -58,6 +60,7 @@ export const initialRoles: RolePermission[] = [
       'overview',
       'new-sale',
       'sales',
+      'quotes',
       'customers',
       'inventory',
       'purchases',
@@ -88,6 +91,7 @@ export const initialRoles: RolePermission[] = [
       'overview',
       'new-sale',
       'sales',
+      'quotes',
       'customers',
       'inventory',
       'purchases',
@@ -113,7 +117,7 @@ export const initialRoles: RolePermission[] = [
     name: 'Sales Executive',
     description: 'High-speed billing POS, customer registry, and invoice creation. Cost prices and profit are restricted.',
     isSystemRole: true,
-    accessibleModules: ['new-sale', 'sales', 'customers', 'invoices'],
+    accessibleModules: ['new-sale', 'sales', 'quotes', 'customers', 'invoices'],
     canViewCostPrice: false,
     canViewProfit: false,
     canEditPrices: false,
@@ -946,6 +950,144 @@ export const initialStockMovements: StockMovement[] = [
 
 export const initialSalesReturns: SalesReturn[] = [];
 
+// -----------------------------------------------------------
+// QUOTATIONS (rate quotes — non-posting)
+// -----------------------------------------------------------
+export const initialQuotes: Quote[] = [
+  {
+    id: 'quote-1',
+    quoteNumber: 'QTN-2026-001',
+    customerId: 'cust-2',
+    customerName: 'Lakshmi Interiors & Plywood',
+    customerPhone: '9845222222',
+    customerGstin: '29PQRSX1234K1Z9',
+    customerAddress: 'Rajajinagar, Bengaluru',
+    date: '2026-09-14T10:00:00Z',
+    validUntil: '2026-09-29T10:00:00Z',
+    items: [
+      {
+        id: 'qitem-1',
+        productId: 'prod-3',
+        productName: 'Asian Paints Apcolite Enamel (4L)',
+        sku: 'PNT-ASN-4',
+        unit: 'Boxes',
+        quantity: 10,
+        purchasePrice: 1050,
+        unitPrice: 1240,
+        discount: 0,
+        discountType: 'fixed',
+        gstRate: 18,
+        taxAmount: 2232,
+        totalAmount: 14632,
+      },
+      {
+        id: 'qitem-2',
+        productId: 'prod-1',
+        productName: 'UltraTech Super Cement (PPC)',
+        sku: 'CEM-ULT-50',
+        unit: 'Bags',
+        quantity: 50,
+        purchasePrice: 340,
+        unitPrice: 378,
+        discount: 0,
+        discountType: 'fixed',
+        gstRate: 28,
+        taxAmount: 5292,
+        totalAmount: 24192,
+      },
+    ],
+    subtotal: 31300,
+    totalDiscount: 0,
+    totalTax: 7524,
+    roundOff: 0,
+    grandTotal: 38824,
+    status: 'Sent',
+    isInterState: false,
+    notes: 'Rate confirmed for September project. Delivery within 2 days of order.',
+    createdBy: 'Priya Patel',
+    createdAt: '2026-09-14T10:00:00Z',
+    updatedAt: '2026-09-14T10:00:00Z',
+  },
+  {
+    id: 'quote-2',
+    quoteNumber: 'QTN-2026-002',
+    customerId: 'cust-1',
+    customerName: 'Karthik Gowda (Vishwa Developers)',
+    customerPhone: '9880112233',
+    customerGstin: '29ABCDE5678G1Z2',
+    customerAddress: 'Site #18, 4th Cross, Vijayanagar, Bengaluru',
+    date: '2026-09-16T09:30:00Z',
+    validUntil: '2026-10-01T09:30:00Z',
+    items: [
+      {
+        id: 'qitem-3',
+        productId: 'prod-2',
+        productName: 'Tata Tiscon 12mm 550D TMT Rebar',
+        sku: 'TMT-TAT-12',
+        unit: 'Kg',
+        quantity: 500,
+        purchasePrice: 58,
+        unitPrice: 66,
+        discount: 0,
+        discountType: 'fixed',
+        gstRate: 18,
+        taxAmount: 5940,
+        totalAmount: 38940,
+      },
+    ],
+    subtotal: 33000,
+    totalDiscount: 0,
+    totalTax: 5940,
+    roundOff: 0,
+    grandTotal: 38940,
+    status: 'Draft',
+    isInterState: false,
+    notes: 'Bulk rate for Vijayanagar site — awaiting their confirmation.',
+    createdBy: 'Rajesh Sharma',
+    createdAt: '2026-09-16T09:30:00Z',
+    updatedAt: '2026-09-16T09:30:00Z',
+  },
+  {
+    id: 'quote-3',
+    quoteNumber: 'QTN-2026-003',
+    customerId: 'cust-4',
+    customerName: 'Meera Electricals',
+    customerPhone: '9880445566',
+    customerGstin: '29JKLMN5678P1Z4',
+    customerAddress: 'Jayanagar 4th Block, Bengaluru',
+    date: '2026-09-10T14:00:00Z',
+    validUntil: '2026-09-17T14:00:00Z',
+    items: [
+      {
+        id: 'qitem-4',
+        productId: 'prod-7',
+        productName: 'Polycab FRLS Copper Wire 1.5 sqmm (90m)',
+        sku: 'WIR-POL-15',
+        unit: 'Boxes',
+        quantity: 8,
+        purchasePrice: 1690,
+        unitPrice: 1950,
+        discount: 0,
+        discountType: 'fixed',
+        gstRate: 18,
+        taxAmount: 2808,
+        totalAmount: 18408,
+      },
+    ],
+    subtotal: 15600,
+    totalDiscount: 0,
+    totalTax: 2808,
+    roundOff: 0,
+    grandTotal: 18408,
+    status: 'Accepted',
+    isInterState: false,
+    notes: 'Awaiting PO copy before billing.',
+    createdBy: 'Amit Kumar',
+    createdAt: '2026-09-10T14:00:00Z',
+    updatedAt: '2026-09-12T11:00:00Z',
+  },
+];
+
 export const initialAuditLogs: AuditLog[] = [
   {
     id: 'log-1',
@@ -995,6 +1137,7 @@ export const getInitialState = (): AppState => ({
   purchases: initialPurchases,
   stockMovements: initialStockMovements,
   salesReturns: initialSalesReturns,
+  quotes: initialQuotes,
   priceHistories: initialPriceHistories,
   auditLogs: initialAuditLogs,
   roles: initialRoles,
diff --git a/src/services/sync/mappers.ts b/src/services/sync/mappers.ts
index 9dbfcfc..f01bd5a 100644
--- a/src/services/sync/mappers.ts
+++ b/src/services/sync/mappers.ts
@@ -16,6 +16,8 @@ import {
   Product,
   Purchase,
   PurchaseItem,
+  Quote,
+  QuoteItem,
   RolePermission,
   Sale,
   SaleItem,
@@ -91,6 +93,7 @@ export const businessToDb = (
     next_payment_number: b.nextPaymentNumber ?? 1,
     next_purchase_number: b.nextPurchaseNumber ?? 1,
     next_return_number: b.nextReturnNumber ?? 1,
+    next_quote_number: b.nextQuoteNumber ?? 1,
     invoice_terms: b.invoiceTerms || '',
     currency_symbol: b.currencySymbol || '₹',
     enable_gst: b.enableGst ?? true,
@@ -130,6 +133,7 @@ export const businessFromDb = (row: DbRow): BusinessProfile => ({
   nextPaymentNumber: num(row.next_payment_number, 1),
   nextPurchaseNumber: num(row.next_purchase_number, 1),
   nextReturnNumber: num(row.next_return_number, 1),
+  nextQuoteNumber: num(row.next_quote_number, 1),
   invoiceTerms: str(row.invoice_terms),
   currencySymbol: str(row.currency_symbol, '₹'),
   enableGst: bool(row.enable_gst, true),
@@ -574,6 +578,95 @@ export const purchaseItemFromDb = (row: DbRow): PurchaseItem => ({
   totalAmount: num(row.total_amount),
 });
 
+// ============================================================
+// QUOTATIONS + QUOTATION ITEMS (non-posting rate quotes)
+// ============================================================
+
+export const quoteToDb = (q: Quote, businessId: string): DbRow => ({
+  id: q.id,
+  business_id: businessId,
+  quote_number: q.quoteNumber,
+  customer_id: q.customerId || null,
+  customer_name: q.customerName,
+  customer_phone: q.customerPhone,
+  customer_address: strOrNull(q.customerAddress),
+  customer_gstin: strOrNull(q.customerGstin),
+  date: iso(q.date),
+  valid_until: q.validUntil ? iso(q.validUntil) : null,
+  subtotal: q.subtotal ?? 0,
+  total_discount: q.totalDiscount ?? 0,
+  total_tax: q.totalTax ?? 0,
+  round_off: q.roundOff ?? 0,
+  grand_total: q.grandTotal ?? 0,
+  status: q.status || 'Draft',
+  is_zero_gst: q.isZeroGst ?? false,
+  is_inter_state: q.isInterState ?? false,
+  notes: strOrNull(q.notes),
+  converted_sale_id: strOrNull(q.convertedSaleId),
+  created_by: q.createdBy,
+  created_at: iso(q.createdAt),
+  updated_at: iso(q.updatedAt),
+});
+
+export const quoteItemToDb = (item: QuoteItem, quoteId: string): DbRow => ({
+  id: item.id,
+  quote_id: quoteId,
+  product_id: item.productId || null,
+  product_name: item.productName,
+  sku: item.sku,
+  unit: item.unit || 'Pieces',
+  quantity: item.quantity,
+  purchase_price: item.purchasePrice ?? 0,
+  unit_price: item.unitPrice,
+  discount: item.discount ?? 0,
+  discount_type: item.discountType || 'fixed',
+  gst_rate: item.gstRate ?? 0,
+  tax_amount: item.taxAmount ?? 0,
+  total_amount: item.totalAmount ?? 0,
+});
+
+export const quoteFromDb = (row: DbRow, items: DbRow[]): Quote => ({
+  id: str(row.id),
+  quoteNumber: str(row.quote_number),
+  customerId: str(row.customer_id),
+  customerName: str(row.customer_name),
+  customerPhone: str(row.customer_phone),
+  customerAddress: row.customer_address ?? undefined,
+  customerGstin: row.customer_gstin ?? undefined,
+  date: iso(row.date),
+  validUntil: row.valid_until ? iso(row.valid_until) : iso(row.date),
+  items: (items || []).map(quoteItemFromDb),
+  subtotal: num(row.subtotal),
+  totalDiscount: num(row.total_discount),
+  totalTax: num(row.total_tax),
+  roundOff: num(row.round_off),
+  grandTotal: num(row.grand_total),
+  status: (str(row.status, 'Draft') as Quote['status']),
+  isZeroGst: bool(row.is_zero_gst),
+  isInterState: bool(row.is_inter_state),
+  notes: row.notes ?? undefined,
+  convertedSaleId: row.converted_sale_id ?? undefined,
+  createdBy: str(row.created_by),
+  createdAt: iso(row.created_at),
+  updatedAt: iso(row.updated_at),
+});
+
+export const quoteItemFromDb = (row: DbRow): QuoteItem => ({
+  id: str(row.id),
+  productId: str(row.product_id),
+  productName: str(row.product_name),
+  sku: str(row.sku),
+  unit: str(row.unit, 'Pieces'),
+  quantity: num(row.quantity),
+  purchasePrice: num(row.purchase_price),
+  unitPrice: num(row.unit_price),
+  discount: num(row.discount),
+  discountType: (str(row.discount_type, 'fixed') as QuoteItem['discountType']),
+  gstRate: num(row.gst_rate),
+  taxAmount: num(row.tax_amount),
+  totalAmount: num(row.total_amount),
+});
+
 // ============================================================
 // STOCK MOVEMENTS
 // ============================================================
